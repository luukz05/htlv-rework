# Backend

Backend simples para centralizar os dados e servir uma API depois.

Endpoints iniciais:

- `GET /health`
- `GET /players`
- `GET /players/:id`
- `GET /players/top`

## Rodar

```bash
npm install
npm run dev
```
